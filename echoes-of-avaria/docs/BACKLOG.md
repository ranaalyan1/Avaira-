# BACKLOG (V1)

1. Create docs and project overview (SPEC + BACKLOG).
2. Define Echo types and their metadata fields (name, description, image URL).
3. Sketch wireframes for Home, Mint, and My Echoes views.
4. Pick wallet connection library and document setup steps.
5. Add Avalanche C-Chain network detection and switch prompt.
6. Implement basic on-chain read calls (supply, token URI, owner).
7. Implement mint flow UI with transaction status states.
8. Build “My Echoes” list using wallet address ownership query.
9. Add Echo detail page with memory entry list.
10. Implement off-chain storage layer (local storage or indexed DB).
11. Add memory entry create/edit/delete UI.
12. Implement JSON export/import for memory file.
13. Validate import schema and handle version mismatch.
14. Add error states and empty states across views.
15. Write README with setup, user flow, and troubleshooting.
